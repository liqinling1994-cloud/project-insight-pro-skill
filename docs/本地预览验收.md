# 本地预览验收

本文用于在推送 GitHub 前检查项目洞察助手PRO的最终展示效果。

## 1. 打开官方展示页

本地文件：

```text
C:\Users\秦岭\ZCodeProject\project-insight-pro-skill\docs\index.html
```

需要看到：

- 首屏有项目洞察助手PRO的定位。
- 小秦配图正常显示。
- 页面包含运行逻辑、输出雷达、BeeGer 样例、安装教程和本地验收区。
- BeeGer 官网链接可以点击打开。

## 2. 检查 BeeGer 样例

输入样例：

```text
C:\Users\秦岭\ZCodeProject\project-insight-pro-skill\examples\beeger-input.md
```

输出样例：

```text
C:\Users\秦岭\ZCodeProject\project-insight-pro-skill\examples\beeger-output.md
```

需要确认：

- BeeGer 以真实产品样例出现，没有抢占项目洞察助手PRO的主定位。
- 样例输出展示了产品定义、用户买到什么、价值边界、适合人群、成交阻力、推广打法和下游指令。
- 样例中保留 BeeGer 官网链接。
- 高风险表达已经降级，没有承诺爆款、成交或增长。

## 3. 检查多平台教程

教程文件：

```text
C:\Users\秦岭\ZCodeProject\project-insight-pro-skill\docs\安装使用.md
```

需要确认：

- 已覆盖 ZCode。
- 已覆盖 Codex。
- 已覆盖 Claude Code / CC。
- 已覆盖 Work Body。
- 每个平台都有安装目录、触发指令和验收方式。

## 4. 检查小秦配图资产

资产目录：

```text
C:\Users\秦岭\ZCodeProject\project-insight-pro-skill\assets\
```

需要确认：

- `hero-xiaoqin-project-map.png`
- `workflow-xiaoqin-pipeline.png`
- `beeger-case-xiaoqin-dashboard.png`
- `output-xiaoqin-handoff.png`

检查标准：

- 小秦是短寸黑发。
- 小秦穿黑西装白衬衫。
- 每张图只有一个主要动作。
- 标签短、少、可读。
- 背景干净，有留白。
- 图片应为生成后的 PNG 成图，不使用手工 SVG 占位图。

## 5. 运行校验脚本

在仓库根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate.ps1
```

通过时输出：

```text
Validation passed: project-insight-pro skill package is complete.
```

## 6. GitHub 推送前确认

本轮只做本地预览，不执行 GitHub 推送。

准备推送前再检查：

- README 是否已经同步本次展示逻辑。
- `docs/index.html` 是否是最终版本。
- `examples/` 是否包含 BeeGer 样例。
- `scripts/validate.ps1` 是否通过。
- Git diff 中没有无关文件和本机隐私路径。
